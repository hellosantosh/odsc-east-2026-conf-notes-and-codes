### Building Effective Agentic Systems

Agentic AI & Workflow Automation

Beginner-Intermediate

Workshop

Prerequisites

Day 2 (April 29), 11:00 AM - 12:00 PM

60 min

In-person

![Tom Haddock](https://staging4.odsc.ai/wp-content/uploads/2026/01/Tom-Haddock-Speakers-Headshots_EAST2026-1.png)

Tom Haddock[](https://www.linkedin.com/in/tom-haddock/)

Forward Deployed Engineer at CrewAI

This session explores practical patterns for designing and deploying effective agentic AI systems with CrewAI, showing how to turn large language models into reliable, production-grade workflows by focusing less on model choice and more on controlling inputs, scoping tasks, and orchestrating agents and tools. 

Notes

- building for fortune 500 companies
- app.crewai.com - free self service platform
- most of this session comes from real production deployments, not synthetic examples
- Tom Haddock specialized in multi-agent systems
- What I will learn today
	- why most enterprise agent projects stall at production delivery
		- and the structural reasons it has nothing to do with model quality
	- common architectural patterns for agent workflows
	- scaling agents for enterprise workloads + hands-on workshop (how to use crewai)
- Every major shift moves business logic to a new layer and the platforms that own that layer define the decade. 
	- client server
		- business logic lives in desktop apps
	- web
		- business logic lives in web servers
	- cloud
		- business logic lives in cloud APIs
	- AI agents
		- business logic lives in autonomous agents
		- agents are the new business logic layer and will define the platforms that own it for the decade. 
- Adhoc workflow
	- things you need done once
	- disposable process
	- output matters
- Embedded workflows
	- workflows embedded into how the business runs
	- recurring and governed
- Building -> Deploying -> Scaling
	- Building the agent is the tax you pay to run it in production
		- if your framework only optimizes for th build, you've optimized it for the cheapest part of the journey
- Where every enterprise agent project gets stuck (see picture)
	- tool sprawl
		- 50+ internal APIs, no governance over which agent can call which
	- prompt injection
		- untrusted user input that quietly steers the agent off-task
	- permissions
		- Marketing's agent shouldn't see Finance's data. But who enforces that?
	- no iteration loop
		- trace it, score it, train on the failures. Most teams have step 1 only. 
	- multi-team operations
		- 5 teams, 5 frameworks, 5 ways to deploy. No central observability.
	- cost runaway
		- unbounded loops, 50-tool context windows, no max_iter discipline. 
		- config should be done for max_guardrails, max_tokens, max_something
	- Crews vs Flows (see picture)
		- Flows allow you to dictate entire workflows
	- Four primitives are
		- Agent, Task (may get rid of this as it has to come from the user but defeats autonomousnnature), Crew, Flow
		- Agent
			- role, goal, backstory, tool
		- Task
			- todo from slides
		- Crew
			- todo
		- Flow
			- todo
	- MCP, in one line of code (see picture)
		- Tools discovered at runtime. Schemas loaded only when invoked, Right-sized per agent. 
		- Runtime discovery
		- Lean context (Dynamic Tool Calling)
			- Attach a 50-tool MCP server. Only tools the agent actually invokes consume tokens. 
		- Right-size per role
			- Research agent gets the full Exa MCP. Outreach agent gets create_draft only
		- Add a tool. No deploy
			- New tool on the MCP server is picked up on the next run. Zero code changes.
	- Most things that look like an agent problem aren't. (see picture)
		- Agents add reasoning loops, latency, cost, and failure surface. Reach for them last, not first - the simplest thing that could work, almost always
		- Single LLM call (includes RAG Q&A)
		- Workflow (orchestrated, predefined path)
		- Agent (dynamic, autonomous)
	- The patterns that separate demos from systems that ship (see picture)
		- All 4 are baked into CrewAI OSS, none require Enterprise. Remember these 4. 
		- Guardrails
			- programmatic. Code validates output
			- LLM-as-judge, probablistic checks
			- Guardrail_max_retries = 3 on failure
		- Flows + State
			- @router for conditional branching
			- Persistent Pydantic state across steps
			- flow.pause() for human-in-the-loop
		- Execution Controls
			- max_iter, max_execution_time, max_tokens per agent
			- max_rpm to rate-limit upstream calls
			- respect_context_window=True for long runs
		- Observability
			- verbose mode + step_callback hooks
			- Native: Arize...
	- A real production app, in roughly ~100 lines of code (see picture)
		- Trigger -> Qualify -> Research -> Human review -> Send. Four MCP servers, zero hardcoded tool schemas. 
	- CrewAI Agentic Stack (see picture)
		- CrewAI enforces consistency across workflows - not just within them
		- Now your problem shifts from Execution layer to Operating layer
	- Sequential: a linear pipeline with a human in the middle (see picture)
	- Procurement automation: 4-stage sequential pipeline (see picture)
		- macro flow is sequential. Inside one stage, parallel data sources fan in. Real architectures combine patterns
	- Four independent verification streams. One synthesized decision. (see picture)
	- Routing (see pic)
	- Stellar: one router, seven automations, one gate per branch (see pic)
	- email: tom@crewai.com
	- Complexity vs Precision (see pic for slide)
		- High complexity, low precision
